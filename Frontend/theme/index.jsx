import { extendTheme } from "@chakra-ui/react";

export const colors = {
  primary: {
    light: "#b1daff",
    dark: "#252934",
  },
  secondary: {
    light: "#90cdf4",
    dark: "#1ca7d0",
  },
};

export default extendTheme({
  colors,
});
